twig:
    paths: ['%kernel.project_dir%/templates']
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    form_themes:
        - 'ttskch_bootstrap_4_horizontal_layout.html.twig'
        - 'ttskch_bootstrap_4_layout.html.twig'  # to be the default theme
